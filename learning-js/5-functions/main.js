// A function needs a name and CAN have a parameter the parameter is not required
function sayHello (name) {
  return 'Hello ' + name;
}

// A function needs to be called to work, the return value can be saved in a variable
var greeting = sayHello ('Mark')

// Output the greeting to an alert